spring:
  datasource:
    url: jdbc:postgresql://localhost:5432
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgres

app:
  security:
    jwt-secret: super-secret
    jwt-expiration-ms: 60000
    jwt-refresh-expiration-ms: 4800000
